# Legacy 4.x documentation

These files describe the unscoped `react-native-esc-pos-printer` 4.x package.
They do not apply to `@countertek/react-native-esc-pos-printer`, which is a
separate Expo module and currently a development scaffold.

See the [current project README](../../README.md).
