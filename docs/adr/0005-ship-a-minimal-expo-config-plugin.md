# Ship A Minimal Expo Config Plugin And Require A Development Build

The package ships a config plugin for Bluetooth, local-network, and USB Discovery permissions plus SDK linkage. Consumers add `@countertek/react-native-esc-pos-printer` to `plugins`. Expo Go is unsupported because the Epson native SDK must be compiled into the app.
