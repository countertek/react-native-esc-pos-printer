# Publish As `@countertek/react-native-esc-pos-printer`

The package is a new Expo module under `@countertek/react-native-esc-pos-printer`, not a 5.0 of the unscoped `react-native-esc-pos-printer` on npm. The unscoped name is still owned by tr3v3r at 4.5.0. This is a clean break: peer `expo`, no Paper/old-arch dual TurboModule, no promise to keep 4.x import paths.
