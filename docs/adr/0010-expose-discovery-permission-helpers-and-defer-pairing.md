# Expose Discovery Permission Helpers And Defer Pairing

The first slice includes runtime helpers to get and request Discovery permissions (Bluetooth / local network / USB as required). `pairBluetoothDevice` is a follow-up issue, not slice 1. The config plugin still writes the static permission entries.
